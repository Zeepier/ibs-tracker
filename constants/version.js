// Bump this on every meaningful deploy. Shown in Settings footer and returned
// by the worker's /version endpoint so "is the new build live?" is answerable
// both in the UI and via curl.
export const APP_VERSION = '1.1.2';
